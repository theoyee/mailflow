import { AdminLicenseManager } from '@/components/AdminLicenseManager';

export const metadata = {
  title: 'Admin Portal | Mailflow',
};

export default function AdminPage() {
  return <AdminLicenseManager />;
}
